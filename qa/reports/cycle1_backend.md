# 生命周期 1 报告 — 后端功能正确性

日期：2026-07-04　·　范围：知识库 / 请求解析 / 智能体规划 / 工具层
测试套件：`qa/test_backend.py`（519 项断言，12 个真实场景 + 边界输入）

## 发现的问题与修复

| # | 严重度 | 问题 | 修复 |
|---|--------|------|------|
| 1 | P0 | 70 个 POI 中 38 个没有配图（首页/行程卡片会出现空白图） | 新增 `scrape_batch2.py`/`scrape_last2.py`，从 Wikimedia Commons 抓取公开授权照片，现已 70/70 全覆盖 |
| 2 | P0 | 「下星期三」被解析成本周三（`_resolve_date` 的 next_week 分支永远不生效） | 重写 weekday 解析：命中「下週/下星期/next week」时跳到下一个日历周对应星期 |
| 3 | P1 | 英文人数解析缺失：「2 people」「we love…」解析成 1 人 | 新增英文人数模式（people/pax/party of N）、中文数字人数（三個人）、we→2 人 |
| 4 | P1 | 多日行程不同天会重复同一景点（如五日游中大三巴出现两次） | `_offline_multi` 逐日累计 `used_ids` 并传入 `exclude_ids`，选点、锚点、指名、导流全部过滤 |
| 5 | P1 | 「避開人潮熱點」核对项在周末场景常挂红（明明已导流仍标 ✗） | 新增错峰调序器 `_tune_crowd_timing`（模拟每站到达时间，热点移到高峰外）；已配对导流的 packed 站点视为已处理 |
| 6 | P2 | 简体「礼拜三」等写法不识别 | weekday 词表补充简体变体 |

## 回归结果

- 修复后重跑：**PASS 519 / FAIL 0 / WARN 0**
- 知识库完整性：70 POI，id 唯一，坐标全部落在所属区域 bbox，开放时间/休息日格式合法，图片 70/70 存在，舊區 25、本地小店 20
- 12 个场景全部产出合法事件流（params→…→result→done），单日无重复景点、时间轴严格递增、不跨岛、23:00 前完结
- 行为验证：周三郑家大屋触发失败恢复且不入行程；周四正常纳入；预算超支自动剔除收费点；热点导流触发；低步行 ≤3.6km；9 种异常输入（空串/emoji/XSS/SQL 注入文本）全部不崩溃

## 下一周期建议

前端 i18n 与静态演示需要同样级别的验证；关注 scenario 图片引用与新增 KB 图片的联动。
